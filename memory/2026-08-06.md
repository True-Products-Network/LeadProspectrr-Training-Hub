# Memory - 2026-08-06

## Session Context
- **User**: Nigel Lear
- **Project**: Property Management Portal (True Products Network)
- **Date**: August 6, 2026

## Recent Work Completed

### User Maintenance Actions
- Fixed activate/suspend/delete action buttons in admin users table
- Created API endpoints:
  - `PUT /api/admin/users/[id]/status` - Update user status (active/suspended)
  - `DELETE /api/admin/users/[id]` - Delete user
- Fixed column name issue: changed from `user_id` to `portal_user_id`
- Added ID fallback logic: tries `portal_user_id` first, then `contact.id`
- Added status validation with allowed values: PENDING, INVITED, ACTIVE, SUSPENDED, REVOKED, INACTIVE

### Site Settings Fix
- Fixed Site Settings page to correctly load/save settings
- Issue was page looking for `result.data` but API returns `result.settings`

### GHL Integration
- Previously added Test Connection button, Tested/Connected badges
- Previously added Disconnect button

### Database-Driven Roles
- Migrated from `portal_roles` to `roles` table
- All 10 roles now in database with UUIDs

## Key Files Modified Today
- `/app/(portal)/admin/users/page.tsx` - Action buttons with handlers
- `/app/api/admin/users/[id]/status/route.ts` - Status update API
- `/app/api/admin/users/[id]/route.ts` - Delete API
- `/app/platform/site-settings/page.tsx` - Settings load/save fix

## Bug List
- **User Status Change**: Status update API returns constraint violation error even with valid status values (NOT_INVITED, PENDING, INVITED, ACTIVE, SUSPENDED, REVOKED, INACTIVE). Needs investigation of database check constraint `contacts_portal_invitation_status_check`.

## Next Steps (from previous context)
1. Fix User Maintenance List - showing 17 records in count but only 1 displaying
2. Menu Access by Role - Update PortalShell/Sidebar based on permissions
3. Permission-Menu Link - Add "Menu Access" column to permissions
4. Test Complete Flow - Platform Admin invites → GHL contact → User resets password → Login

## Technical Notes
- contacts table uses `portal_user_id` (not `user_id`)
- `portal_roles` renamed to `portal_roles_backup`
- `user_roles_legacy` has RLS disabled to prevent infinite recursion
- System roles (`is_system_role = true`) cannot be deleted but can have permissions edited
